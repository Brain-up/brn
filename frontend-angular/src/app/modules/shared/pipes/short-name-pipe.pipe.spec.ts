import { ShortNamePipe } from "./short-name-pipe.pipe";

describe('ShortNamePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ShortNamePipe();
    expect(pipe).toBeTruthy();
  });
});
