import RouteTemplate from 'ember-route-template';
// eslint-disable-next-line @typescript-eslint/no-unused-vars
import Profile from 'brn/components/profile';

export default RouteTemplate(
  <template>
    <Profile />
  </template>
);
