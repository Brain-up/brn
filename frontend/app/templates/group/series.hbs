<div>
  {{#if (is-active "group.series.index")}}
    {{page-title @model.name}}
    <div class="sm:grid-cols-4 gap-y-2 sm:gap-y-3 grid grid-cols-3 mx-2 mb-4">
      {{#each this.exerciseSubGroups as |group|}}
        <ExerciseGroup @group={{group}} />
      {{/each}}
    </div>
  {{/if}}
  {{outlet}}
</div>