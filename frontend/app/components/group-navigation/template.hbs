<ul class="group-nav-list mt-4">
  {{#each (sort-by "id" @group.series) as |series|}}
    <li>
      {{#link-to "series" series}}
        {{series.name}}
      {{/link-to}}
    </li>
  {{/each}}
</ul>