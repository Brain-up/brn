import deepClone from 'brn/utils/deep-clone';
import { module, test } from 'qunit';

module('Unit | Utility | deepClone', function() {
  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = deepClone();
    assert.ok(result);
  });
});
