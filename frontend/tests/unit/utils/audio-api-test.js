import audioApi from 'brn/utils/audio-api';
import { module, test } from 'qunit';

module('Unit | Utility | audio-api', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = audioApi();
    assert.ok(result);
  });
});
