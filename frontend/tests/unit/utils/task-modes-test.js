import { MODES } from 'brn/utils/task-modes';
import { module, test } from 'qunit';

module('Unit | Utility | task-modes', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    assert.ok(MODES);
  });
});
