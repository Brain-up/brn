import fileUrl from 'brn/utils/file-url';
import { module, test } from 'qunit';

module('Unit | Utility | file-url', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = fileUrl();
    assert.ok(result);
  });
});
